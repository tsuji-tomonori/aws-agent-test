---
title: 6. 独立試行を繰り返す
description: 同じケースを新しい会話・workspaceで5回実行し、一度の成功と安定性を区別します。
---

import { Steps, Tabs, TabItem, Aside } from '@astrojs/starlight/components';
import FileCreate from '../../components/FileCreate.astro';
import fileContent1 from '../../snippets/scripts/summarize.py?raw';
import TaskMeta from '../../components/TaskMeta.astro';
import ConsoleStep from '../../components/ConsoleStep.astro';
import Command from '../../components/Command.astro';
import ExpectedResult from '../../components/ExpectedResult.astro';
import Checkpoint from '../../components/Checkpoint.astro';

<TaskMeta time="20–40分" level="中級" credentials="Agent CLI認証" cost="5 trial分の推論費用" />

1回の成功はデモにはなりますが、運用可能性の証明にはなりません。同一条件を5回繰り返し、成功と失敗を都合よく選ばず、すべて保存します。

<Steps>

1. ### 変更しない条件を確認する

   model、CLI version、prompt、case、MCP、権限、出力Schema、timeoutを固定します。途中で変更した場合はrunを止め、新しい`experiment_id`で最初から測ります。

2. ### 5個の独立workspaceを作って実行する

   使用するCLIのタブを一つ選びます。第5章のpilotとは別の`no-mcp-batch-01`へ保存するため、pilotを上書きしません。

   <Tabs>
     <TabItem label="Codex">
       <Command title="5回の独立trialを実行" prompt="BASH">

       ```bash
       pricing_disable=()
       if codex mcp get aws-pricing > /dev/null 2>&1; then
         pricing_disable=(-c mcp_servers.aws-pricing.enabled=false)
       fi
       case_id=cfn-missing-usage
       case_dir="workshop/manual/cases/$case_id"
       run_dir="workshop/manual/runs/no-mcp-batch-01/$case_id"
       # 同名runの上書きを防ぐ。再測定時は新しいrun名にする。
       if test -e "$run_dir"; then
         printf '%s\n' "既存runです: $run_dir"
       else
         mkdir -p "$run_dir"
         cp workshop/manual/experiment.json "$run_dir/experiment.json"
         for number in 01 02 03 04 05; do
           trial="$run_dir/trial-$number"
           mkdir -p "$trial/input"
           cp "$case_dir/case-public.json" "$trial/case-public.json"
           cp "$case_dir/input/template.yaml" "$trial/input/template.yaml"
           cp workshop/manual/prompts/agent-v1.md "$trial/TASK.md"
           cp workshop/manual/schemas/agent-output.schema.json "$trial/agent-output.schema.json"
           (
             cd "$trial"
             codex exec --ephemeral --sandbox read-only --json \
               "${pricing_disable[@]}" \
               --output-schema agent-output.schema.json \
               --output-last-message answer.json \
               - < TASK.md > events.jsonl
           )
           agent_exit=$?
           printf '%s\n' "$agent_exit" > "$trial/exit-code.txt"
           if test "$agent_exit" -eq 0 && test -s "$trial/answer.json"; then
             python workshop/manual/scripts/check_result.py \
               "$trial/answer.json" "$case_dir/oracle.json" > "$trial/verdict.json" || true
           else
             printf '%s\n' '{"passed": false, "errors": ["agent execution failed"]}' > "$trial/verdict.json"
           fi
         done
       fi
       ```

       </Command>
     </TabItem>
     <TabItem label="Claude Code">
       <Command title="5回の独立trialを実行" prompt="BASH">

       ```bash
       case_id=cfn-missing-usage
       case_dir="workshop/manual/cases/$case_id"
       run_dir="workshop/manual/runs/no-mcp-batch-01/$case_id"
       # 同名runの上書きを防ぐ。再測定時は新しいrun名にする。
       if test -e "$run_dir"; then
         printf '%s\n' "既存runです: $run_dir"
       else
         mkdir -p "$run_dir"
         cp workshop/manual/experiment.json "$run_dir/experiment.json"
         for number in 01 02 03 04 05; do
           trial="$run_dir/trial-$number"
           mkdir -p "$trial/input"
           cp "$case_dir/case-public.json" "$trial/case-public.json"
           cp "$case_dir/input/template.yaml" "$trial/input/template.yaml"
           cp workshop/manual/prompts/agent-v1.md "$trial/TASK.md"
           cp workshop/manual/schemas/agent-output.schema.json "$trial/agent-output.schema.json"
           (
             cd "$trial"
             claude -p --max-turns 24 --output-format json \
               --no-session-persistence --permission-mode dontAsk \
               --tools "Read,Glob,Grep"  \
               --strict-mcp-config --mcp-config '{"mcpServers":{}}' \
               --json-schema "$(cat agent-output.schema.json)" \
               "$(cat TASK.md)" > claude-result.json
           ) && python workshop/manual/scripts/extract_claude.py \
             "$trial/claude-result.json" "$trial/answer.json"
           agent_exit=$?
           printf '%s\n' "$agent_exit" > "$trial/exit-code.txt"
           if test "$agent_exit" -eq 0 && test -s "$trial/answer.json"; then
             python workshop/manual/scripts/check_result.py \
               "$trial/answer.json" "$case_dir/oracle.json" > "$trial/verdict.json" || true
           else
             printf '%s\n' '{"passed": false, "errors": ["agent execution failed"]}' > "$trial/verdict.json"
           fi
         done
       fi
       ```

       </Command>
     </TabItem>
   </Tabs>

3. ### 成功率と信頼区間を計算する

   `workshop/manual/scripts/summarize.py`を作ります。

   <FileCreate path="workshop/manual/scripts/summarize.py" lang="python" code={fileContent1} />

   <Command title="反復結果を集計">

   ```bash
   python workshop/manual/scripts/summarize.py \
     workshop/manual/runs/no-mcp-batch-01/cfn-missing-usage \
     > workshop/manual/runs/no-mcp-batch-01/cfn-missing-usage/summary.json
   ```

   </Command>

4. ### 4つの指標を読み分ける

   <ConsoleStep title="指標を運用上の問いへ翻訳する" label="ANALYSIS">

   | 指標 | このケースで答える問い |
   |---|---|
   | success rate | 5回のうち何回、全ゲートを通ったか |
   | Wilson 95% CI | 少数試行の成功率をどこまで信用できるか |
   | pass@5 | 5回のうち一度でも成功したか |
   | pass^5 | 5回すべて成功したか |

   </ConsoleStep>

   人手が毎回再試行できる試作品ではpass@kも参考になります。無人運用を考えるなら、すべて成功するpass^kを重く見ます。

5. ### 失敗を分類する

   各`verdict.json`と`events.jsonl`から、execution、timeout、JSON、service、arithmetic、source、clarificationへ分類します。単に「LLMが不安定」とまとめず、prompt、tool、case、modelのどこを直すべきか結び付けます。

6. ### MCPありケースも同じ回数で測る

   第4章を実施した場合だけ、以下のCLIタブを一つ選んで実行します。保存先は`pricing-mcp-batch-01`です。

   <Tabs>
     <TabItem label="Codex">
       <Command title="MCPありの5回反復" prompt="BASH">

       ```bash
       case_id=cfn-static-site
       case_dir="workshop/manual/cases/$case_id"
       run_dir="workshop/manual/runs/pricing-mcp-batch-01/$case_id"
       # 同名runの上書きを防ぐ。再測定時は新しいrun名にする。
       if test -e "$run_dir"; then
         printf '%s\n' "既存runです: $run_dir"
       else
         mkdir -p "$run_dir"
         cp workshop/manual/experiment-pricing-mcp.json "$run_dir/experiment.json"
         for number in 01 02 03 04 05; do
           trial="$run_dir/trial-$number"
           mkdir -p "$trial/input"
           cp "$case_dir/case-public.json" "$trial/case-public.json"
           cp "$case_dir/input/template.yaml" "$trial/input/template.yaml"
           cp workshop/manual/prompts/agent-v1.md "$trial/TASK.md"
           cp workshop/manual/schemas/agent-output.schema.json "$trial/agent-output.schema.json"
           (
             cd "$trial"
             codex exec --ephemeral --sandbox read-only --json \
               -c mcp_servers.aws-pricing.enabled=true \
               --output-schema agent-output.schema.json \
               --output-last-message answer.json \
               - < TASK.md > events.jsonl
           )
           agent_exit=$?
           printf '%s\n' "$agent_exit" > "$trial/exit-code.txt"
           if test "$agent_exit" -eq 0 && test -s "$trial/answer.json"; then
             python workshop/manual/scripts/check_result.py \
               "$trial/answer.json" "$case_dir/oracle.json" > "$trial/verdict.json" || true
           else
             printf '%s\n' '{"passed": false, "errors": ["agent execution failed"]}' > "$trial/verdict.json"
           fi
         done
       fi
       ```

       </Command>
     </TabItem>
     <TabItem label="Claude Code">
       <Command title="MCPありの5回反復" prompt="BASH">

       ```bash
       case_id=cfn-static-site
       case_dir="workshop/manual/cases/$case_id"
       run_dir="workshop/manual/runs/pricing-mcp-batch-01/$case_id"
       # 同名runの上書きを防ぐ。再測定時は新しいrun名にする。
       if test -e "$run_dir"; then
         printf '%s\n' "既存runです: $run_dir"
       else
         mkdir -p "$run_dir"
         cp workshop/manual/experiment-pricing-mcp.json "$run_dir/experiment.json"
         for number in 01 02 03 04 05; do
           trial="$run_dir/trial-$number"
           mkdir -p "$trial/input"
           cp "$case_dir/case-public.json" "$trial/case-public.json"
           cp "$case_dir/input/template.yaml" "$trial/input/template.yaml"
           cp workshop/manual/prompts/agent-v1.md "$trial/TASK.md"
           cp workshop/manual/schemas/agent-output.schema.json "$trial/agent-output.schema.json"
           mcp_config="$(pwd)/workshop/manual/config/pricing-mcp.json"
           (
             cd "$trial"
             claude -p --max-turns 24 --output-format json \
               --no-session-persistence --permission-mode dontAsk \
               --tools "Read,Glob,Grep" --allowedTools "mcp__aws-pricing__*" \
               --strict-mcp-config --mcp-config "$mcp_config" \
               --json-schema "$(cat agent-output.schema.json)" \
               "$(cat TASK.md)" > claude-result.json
           ) && python workshop/manual/scripts/extract_claude.py \
             "$trial/claude-result.json" "$trial/answer.json"
           agent_exit=$?
           printf '%s\n' "$agent_exit" > "$trial/exit-code.txt"
           if test "$agent_exit" -eq 0 && test -s "$trial/answer.json"; then
             python workshop/manual/scripts/check_result.py \
               "$trial/answer.json" "$case_dir/oracle.json" > "$trial/verdict.json" || true
           else
             printf '%s\n' '{"passed": false, "errors": ["agent execution failed"]}' > "$trial/verdict.json"
           fi
         done
       fi
       ```

       </Command>
     </TabItem>
   </Tabs>

   <Command title="MCPあり結果を集計" prompt="BASH">

   ```bash
   python workshop/manual/scripts/summarize.py \
     workshop/manual/runs/pricing-mcp-batch-01/cfn-static-site \
     > workshop/manual/runs/pricing-mcp-batch-01/cfn-static-site/summary.json
   ```

   </Command>

</Steps>

<Aside type="note" title="5/5でも真の成功率100%とは限らない">
  少数試行では区間が広いため、success rateとWilson 95%信頼区間を必ず併記します。試行数は推論費用と必要な確度から決めます。
</Aside>

<ExpectedResult>

5個の独立trial、raw event、最終回答、決定的判定、`summary.json`が残り、一度の成功と反復安定性を区別できます。

</ExpectedResult>

<Checkpoint>

- [ ] trialごとに新しいworkspaceと会話を使った
- [ ] 失敗trialも削除せず保存した
- [ ] success rateとWilson CIを併記した
- [ ] pass@kとpass^kを区別した
- [ ] 途中で条件を変えていない

</Checkpoint>
